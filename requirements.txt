# ==================================================
# TensorFlow Requirements for BIM Sign Language Recognition
# MobileNetV3-Small with MediaPipe Hand Detection
# ==================================================
#
# IMPORTANT Windows GPU Support (RTX 4060)
# ============================================
# TensorFlow 2.15.0 does NOT support native Windows GPU.
# You MUST run this inside WSL2 (Windows Subsystem for Linux)
# to use your RTX 4060 GPU acceleration.
#
# Without WSL2 Training will use CPU only (~several hours)
# With WSL2    Training will use GPU (~30 minutes)
#
# WSL2 Setup httpslearn.microsoft.comen-uswindowswslinstall
# ==================================================

# TensorFlow (CPU-only on Windows)
# For GPU support, use WSL2 with tensorflow[and-cuda]==2.15.0
tensorflow==2.15.0

# Keras (must match TensorFlow version for two-stage training)
keras==2.15.0

# Hand landmark detection & ROI extraction
mediapipe==0.10.9
opencv-python==4.9.0.80

# Protobuf (pinned to prevent MediaPipeTensorFlow conflicts)
protobuf==3.20.3

# Progress bars
tqdm==4.66.1

# ==================================================
# Original packages (kept from previous setup)
# ==================================================

contourpy==1.3.1
cycler==0.12.1
filelock==3.16.1
fonttools==4.55.3
fsspec==2024.12.0
Jinja2==3.1.5
joblib==1.4.2
kiwisolver==1.4.8
MarkupSafe==3.0.2
matplotlib==3.10.0
mpmath==1.3.0
networkx==3.4.2
numpy==1.26.4
packaging==24.2
pandas==2.2.3
pillow==11.0.0
pyparsing==3.2.0
python-dateutil==2.9.0.post0
pytz==2024.2
scikit-learn==1.6.0
scipy==1.14.1
seaborn==0.13.2
setuptools==75.6.0
six==1.17.0
sympy==1.13.1
threadpoolctl==3.5.0
typing_extensions==4.12.2
tzdata==2024.2
